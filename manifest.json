{
  "manifest_version": 2,

  "name": "Better Dark Mode",
  "description": "Gives pages a better dark mode by avoiding things like full white text on full black test which let's admit, isn't much better on the eyes.",
  "version": "1.0",

  "browser_action": {
   "default_icon": "icon.png",
   "default_popup": "popup.html"
  },
  "permissions": [
   "activeTab"
   ],
   "content_scripts": [
  {
    "matches": [
      "<all_urls>"
    ],
    "js": ["insert.js"]
  }
]
}
